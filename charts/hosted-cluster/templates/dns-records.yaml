{{- if .Values.dns.enabled }}
{{- $clusterName := required "clusterName is required" .Values.clusterName }}
{{- $baseDomain := include "hosted-cluster.baseDomain" . }}
{{- $kubeAPIServerDNSName := include "hosted-cluster.kubeAPIServerDNSName" . }}

{{- if .Values.dns.autoGenerate }}
{{- $allDefaults := include "hosted-cluster.serviceDefaults" . | fromYaml }}
{{- $svcDef := index $allDefaults "APIServer" }}
{{- $apiConfig := dig "APIServer" dict .Values.services }}
{{- $internalHost := include "hosted-cluster.serviceHostname" (dict "serviceConfig" $apiConfig "prefix" $svcDef.prefix "facing" $svcDef.facing "clusterName" $clusterName "ctx" $) }}
{{- if and $apiConfig.ipAddress $internalHost }}
---
apiVersion: v1
kind: Service
metadata:
  name: dns-apiserver-internal-{{ $clusterName }}
  namespace: {{ $.Values.namespace }}
  labels:
    massopen.cloud/external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/hostname: {{ $internalHost | quote }}
    external-dns.alpha.kubernetes.io/target: {{ $apiConfig.ipAddress | quote }}
spec:
  type: ExternalName
  externalName: placeholder.invalid
{{- end }}
{{- $apiTarget := or $apiConfig.externalIpAddress $apiConfig.ipAddress }}
{{- if $apiTarget }}
---
apiVersion: v1
kind: Service
metadata:
  name: dns-apiserver-external-{{ $clusterName }}
  namespace: {{ $.Values.namespace }}
  labels:
    massopen.cloud/external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/hostname: {{ $kubeAPIServerDNSName | quote }}
    external-dns.alpha.kubernetes.io/target: {{ $apiTarget | quote }}
spec:
  type: ExternalName
  externalName: placeholder.invalid
{{- end }}

{{- /* Guest cluster ingress wildcard. The guest router is published as a
LoadBalancer with dnsManagementPolicy Unmanaged, so nothing else creates its
DNS record. The LoadBalancer IP itself is pinned by MetalLB on the hosted
cluster (guest-side config), not here; we only publish the wildcard pointing at
the externally reachable IP (falling back to ipAddress for internal-only
clusters). */}}
{{- $ingress := .Values.ingress | default dict }}
{{- $ingressTarget := or $ingress.externalIpAddress $ingress.ipAddress }}
{{- if $ingressTarget }}
{{- $ingressHost := $ingress.hostname | default (printf "*.apps.%s.%s" $clusterName $baseDomain) }}
---
apiVersion: v1
kind: Service
metadata:
  name: dns-ingress-{{ $clusterName }}
  namespace: {{ $.Values.namespace }}
  labels:
    massopen.cloud/external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/hostname: {{ $ingressHost | quote }}
    external-dns.alpha.kubernetes.io/target: {{ $ingressTarget | quote }}
spec:
  type: ExternalName
  externalName: placeholder.invalid
{{- end }}
{{- end }}

{{- range .Values.dns.records }}
---
apiVersion: v1
kind: Service
metadata:
  name: dns-{{ .name }}-{{ $clusterName }}
  namespace: {{ $.Values.namespace }}
  labels:
    massopen.cloud/external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/hostname: {{ .hostname | quote }}
    {{- if and .target .targets }}
    {{- fail (printf "dns record %q: specify either target or targets, not both" .name) }}
    {{- else if and (not .target) (not .targets) }}
    {{- fail (printf "dns record %q: one of target or targets is required" .name) }}
    {{- end }}
    {{- if .targets }}
    external-dns.alpha.kubernetes.io/target: {{ .targets | join "," | quote }}
    {{- else }}
    external-dns.alpha.kubernetes.io/target: {{ .target | quote }}
    {{- end }}
spec:
  type: ExternalName
  externalName: placeholder.invalid
{{- end }}
{{- end }}
