apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  annotations:
    # The subchart's resources carry no wave, so they land in wave 0. The
    # namespace has to exist first, hence the negative wave.
    argocd.argoproj.io/sync-wave: "-1"
spec: {}
