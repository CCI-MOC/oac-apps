apiVersion: v2
name: grafana-operator
description: Install the Grafana operator
type: application
version: 0.1.0
dependencies:
  - name: operator-library
    version: "0.1.0"
    repository: "file://../operator-library"
